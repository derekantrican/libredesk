# Railway config-as-code: build from source with Dockerfile.railway instead of the upstream
# Dockerfile (which expects a prebuilt binary).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"
